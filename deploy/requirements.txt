# BLE Simulator Web Service Dependencies

# BLE Communication
bleak                            # Bluetooth Low Energy library
# pybluez                        # Classic Bluetooth library (use system package)

# Web Framework
fastapi                          # Modern web framework
uvicorn[standard]                # ASGI server with standard extras
flask                            # Traditional web framework
werkzeug                         # WSGI utility library

# Data Validation and Serialization
pydantic                         # Data validation using Python type hints

# HTTP and CORS
fastapi-cors                     # CORS middleware for FastAPI (alternative to built-in)

# Jinja2 Templates (Flask dependency)
jinja2                           # Template engine

# Development and Production
gunicorn                         # Production WSGI server
uvloop                           # Ultra fast asyncio event loop (Unix only)
httptools                        # Fast HTTP parser

# Utilities
python-dotenv                    # Environment configuration

# BLE 模拟器依赖
# 注意：这些库需要系统级依赖，请先运行 deploy/install_dependencies.sh
pydbus>=0.6.0                    # D-Bus通信
pygobject>=3.36.0                # GLib事件循环
dbus-python>=1.2.16              # Python D-Bus绑定

# 替代方案：如果上述安装失败，可以使用系统包
# sudo apt install python3-gi python3-dbus python3-bluez
# 然后在虚拟环境中创建符号链接：
# ln -s /usr/lib/python3/dist-packages/gi ~/.venv/lib/python3.x/site-packages/
# ln -s /usr/lib/python3/dist-packages/dbus ~/.venv/lib/python3.x/site-packages/